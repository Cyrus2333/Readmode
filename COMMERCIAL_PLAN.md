# Readmode 商业化与上线方案

## 一句话方案

**免费本地阅读器 + 一次性 Pro 授权 + 第三方支付 + 离线签名激活码 + 静态官网。**

Readmode 插件运行时不依赖自建后端，不上传文档，不要求账号，也不做每次打开文件时的在线许可证校验。

## 产品边界

### Free

- 本地 Markdown / HTML 文件；
- 已登录状态下的在线 Markdown / HTML 附件链接；
- 全屏 Markdown 阅读布局和永久左侧目录；
- 表格、任务列表、提示块、图片、链接、代码块；
- 浅色 / 深色主题、查看原文、Markdown 打印；
- HTML 安全预览；
- 用户主动选择后运行原始 HTML。

### Pro（当前已实现）

- Markdown 自定义字体；
- Markdown 正文大小；
- Markdown 正文行距；
- 离线签名激活。

### Pro（后续候选）

- Mermaid 和数学公式；
- HTML 项目目录预览（CSS / JS / 图片资源）；
- 设备尺寸和响应式原型预览；
- 本地截图导出；
- 更多主题和代码阅读样式；
- 最近文档、阅读位置和偏好导入导出。

不要收费阻断基础文件阅读路径。先用免费版本验证重复使用，再围绕真实频繁场景扩展 Pro。

## 商业基础设施

```text
Chrome Web Store：分发
静态网站：介绍、SEO、隐私、条款、帮助、购买入口
第三方支付：收款、订单、税务/收据能力（取决于平台）
离线许可证：本地签发和本地校验
```

GitHub Pages 不是插件运行依赖，只是早期静态网站的低成本部署方式。后续可以绑定自己的域名。

## 许可证

当前实现使用 P-256 / ECDSA-SHA-256。插件只包含公钥，签发私钥由负责人离线保存。详见 `docs/licensing.md`。

当前仓库内的公钥只用于开发验证，**不能直接用于正式售卖**。正式收费前必须：

1. 生成生产密钥；
2. 替换 `src/pro/license.js` 的公钥；
3. 发布包含生产公钥的新版本；
4. 进行真实小额订单、激活、篡改和退款流程测试。

## 分阶段上线

1. **预发布**：完成阅读体验、权限/隐私审计、离线许可证和静态站点；不收真实款。
2. **免费公开**：上架 Chrome Web Store，收集本地 Markdown、TAPD 附件和 HTML 原型案例。
3. **手动付费验证**：第三方支付平台收款，人工核对订单并用本地脚本发放签名许可证。
4. **按需自动化**：订单量证明必要后，再增加 serverless 发码或支付 Webhook，不建设文档 SaaS。

## 发布前必须由负责人提供

- 正式域名或 GitHub Pages 地址；
- 支持邮箱或 issue tracker；
- Chrome Web Store 开发者账号；
- 首个支付平台、货币和销售地区；
- 首个 Pro 价格、退款期限和授权规则；
- 生产许可证密钥的安全保管位置；
- 3–5 张不含敏感信息的最终截图。
